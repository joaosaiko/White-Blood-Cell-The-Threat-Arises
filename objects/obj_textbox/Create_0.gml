/// @description Inserir descrição aqui
text = "teste dialogo";
page = 0;
charCount = 0;

xBuffer = 10;
yBuffer = 30;

creator = noone;
name = noone;

boxWidth = sprite_get_width(spr_box);  // 20px de margem em cada lado
boxHeight = sprite_get_height(spr_box);  // Altura da caixa de diálogo

stringHeigth = string_height(text);

